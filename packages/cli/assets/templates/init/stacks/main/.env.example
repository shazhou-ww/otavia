# Copy to .env in this stack directory (`otavia setup` creates .env from this file when missing).
# Cloud identity (optional; `otavia setup` can prompt interactively):
# AWS_PROFILE=
# AZURE_SUBSCRIPTION_ID=
# Optional isolated Azure CLI config directory:
# AZURE_CONFIG_DIR=
# Example app vars:
# API_KEY=
